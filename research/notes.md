→ normalize
→ tokenize
→ remove stopwords
→ remove weak tokens
→ generate:
    - unigrams
    - adjacent bigrams
    - pattern combos
→ match:
    - bigrams first
    - unigrams next
→ dedupe + limit
